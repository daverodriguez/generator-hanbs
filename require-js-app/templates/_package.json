{
	"name": "<%= _.slugify(appName) %>",
	"version": "0.1.0",
	"description": "A HanBootStrap/RequireJS application",
	"main": "main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"grunt-contrib-connect": "~0.5.0",
		"grunt": "~0.4.1",
		"grunt-contrib-requirejs": "~0.4.1"
	}
}
